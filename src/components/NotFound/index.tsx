import React from "react";

export interface Props {
  name: string;
}

const Greeting = ({ name }: Props) => {
  return (
    <React.Fragment>
      404
    </React.Fragment>
  );
};

export default Greeting;
